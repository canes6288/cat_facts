// This initializes the Angular project
// 'templates' is an Angular module
// We downloaded this by adding it to our Gemfile
// We loaded it in via the Asset Pipeline in application.js
// This name needs to align with the one next to ng-app in
// app/views/layouts/application.html.erb
angular.module('catFacts', ['templates'])
